# Template only — copy to `.env` locally, never commit real values.
# `.env` is already git-ignored (see root .gitignore).
#
# Only the variables the app actually reads today (validated in
# src/config/env.validation.ts). No database/JWT/Firebase credentials
# yet — those are added when the module that needs them is
# implemented, not before (see apps/backend/src/modules/core/README.md).

NODE_ENV=development
PORT=3000
