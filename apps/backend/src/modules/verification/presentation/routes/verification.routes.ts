/**
 * Centralized route path constants for the Verification controller.
 */
export const VerificationRoutes = {
  base: 'verifications',
  search: 'search',
  byId: ':id',
  document: ':id/document',
} as const;
