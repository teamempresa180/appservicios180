/**
 * The kind of classification a Category represents. A plain label — no
 * hierarchy, no tree, no dynamic categorization logic.
 */
export enum CategoryType {
  Standard = 'STANDARD',
  Premium = 'PREMIUM',
  Seasonal = 'SEASONAL',
  Other = 'OTHER',
}
