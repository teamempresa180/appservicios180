/**
 * The general nature of a Chat conversation. A plain label — no messaging
 * logic, no real-time transport.
 */
export enum ChatType {
  OrderRelated = 'ORDER_RELATED',
  Support = 'SUPPORT',
  Other = 'OTHER',
}
