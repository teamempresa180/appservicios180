/**
 * Centralized route path constants for the Notification controller.
 */
export const NotificationRoutes = {
  base: 'notifications',
  search: 'search',
  byId: ':id',
  read: ':id/read',
} as const;
